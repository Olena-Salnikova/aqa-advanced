// Declare a variable for age
const age = 27;

// Boolean variable to check if the person is an adult (18 or older)
const isAdult = age >= 18;

// Output variables to the console
console.log("Age:", age);
console.log("Is adult:", isAdult);